name: session_repository
description: "Dart package which manages the sessions."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.1 <4.0.0'
  
dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  cloud_firestore: ^4.15.8

# custom 
  child_repository:
    path: ../child_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
uses-material-design: true