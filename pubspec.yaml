name: perfect_childcare
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.1 <4.0.0'
  
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.1.2
  flutter_dotenv: ^5.1.0
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  bloc: ^8.1.3
  flutter_bloc: ^8.1.4
  equatable: ^2.0.5

  #custom repositories
  user_repository:
    path: packages/user_repository
  child_repository:
    path: packages/child_repository
  session_repository:
    path: packages/session_repository
    
  
  syncfusion_flutter_sliders: ^26.1.35
  persistent_bottom_nav_bar: ^5.0.2
  connectivity_plus: ^6.0.3
  http: ^1.2.0
  auto_size_text: ^3.0.0
  image_picker: ^1.0.8
  image_cropper: ^5.0.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/icons/